<%= t(:reference) %>
<%= @item.ref %>

<%= t(:title) %>
<%= @item.title %>

<%= t(:price) %>
<%= @item.price %>

<%= t(:details) %>
<%= @item.details %>

<div>
	<%= link_to t(:add_to_cart), add_to_cart_item_path(@item), method: 'post' %>
</div>
<div>
	<%= link_to t(:continue_shopping), root_path %>
</div>